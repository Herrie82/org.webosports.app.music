# generated build artifacts
build-output/
*.ipk

# compiled backend binary (built by deploy scripts)
spotify-webos-service
service/spotify-webos-service

# on-device secrets — NEVER commit (these live on the device at /media/internal)
*-token.json
spotify-token.json
qobuz-auth
deezer-arl
tidal-token
*.arl
spotify-client-id
device.wvd
jamendo-clientid

# editor / OS
.DS_Store
*.swp
*~

# tooling
.claude/
node_modules/

# upstream librespot build checkout (has its own .git; 747M) — kept locally for
# builds, not committed. Our cross-compile config is preserved in librespot/cargo-config.toml.
librespot-src/

# decompiled proprietary MPR reference (Hedami) — kept locally, not published
reference/
